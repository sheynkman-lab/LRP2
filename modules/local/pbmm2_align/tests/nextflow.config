process {
    withName: PBMM2_ALIGN {
        ext.args = "--preset ISOSEQ --sort"
        memory = 10.GB
        cpus = 2
        publishDir = [
            enabled: false
        ]
    }
}

// Disable publishDir for tests
params {
    outdir = "test_output"
}
